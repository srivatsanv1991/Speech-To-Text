# Speech-To-Text
This Program will use AWS Transcribe to convert the speech to text

Run maven install to get the all the dependencies

Deploy the war to Tomcat server to run the Application

set environment variable for your aws keys
